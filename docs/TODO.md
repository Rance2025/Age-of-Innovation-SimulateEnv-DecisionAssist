# 游戏开发待办事项

## 前端界面

### 地图增量渲染
- [ ] 侧楼建筑增量更新按独立渲染单元处理
  - `map_grid` 前端渲染按 3 部分理解：地块、主建筑、是否有侧楼
  - `terrain` 只刷新六边形地块颜色
  - `controller + building_id + is_neutral` 作为主建筑整体重绘
  - `has_annex` 单独控制侧楼叠层的增删，暂缓实现

### 游戏菜单功能
- [x] 右上角选择按钮去掉边框
- [x] 点击按钮使用 Modal 弹窗组件
- [x] 添加"结束游戏"按钮 - 返回主界面
- [x] 添加"重新设置"按钮 - 返回设置页面，保留当前设置
- [ ] **重新开始** - 在当前界面重启游戏（需要后端支持）
  - 需要后端 API 支持重置游戏状态
  - 前端需要重新初始化游戏数据
  - 考虑是否需要确认对话框

### 初始设置页面
- [ ] **电脑策略选择**
  - 简单模式：AI 随机选择行动
  - 普通模式：AI 基于基础策略
  - 困难模式：AI 使用优化算法
  - 需要设计策略选择 UI（下拉框或按钮组）
  - 需要将策略参数发送到后端

- [ ] **自定义游戏模式弹窗**
  - 自定义地图配置（可选地形、大小）
  - 自定义胜利条件
  - 自定义初始资源
  - 需要设计弹窗内的表单布局
  - 需要保存自定义配置到本地存储

## 后端接口

### 游戏控制接口
- [ ] `POST /api/game/restart` - 重新开始游戏
  - 重置游戏状态到初始状态
  - 保留当前玩家设置（玩家数量、派系等）
  - 返回新的游戏会话 ID

### AI 策略接口
- [ ] `POST /api/game/ai-strategy` - 设置 AI 策略
  - 接收策略类型参数（easy/normal/hard）
  - 应用到对应电脑玩家

## 其他待办

- [ ] 添加游戏音效
- [ ] 优化移动端适配
- [ ] 添加游戏存档功能

## 游戏记录与历史记录

> **状态**：规划中，按“游戏结束后自动保存记录，随后可在历史记录中查看”方向推进

### 核心目标
- [ ] 游戏结束后自动保存当前对局记录
  - 保存时机明确为“游戏正常结束”后自动触发
  - 保存成功后可在历史记录列表中查看该局
  - 需要明确失败重试或保存失败提示策略
- [ ] 增加历史记录入口与列表页
  - 展示已保存对局的基础信息
  - 支持按时间倒序查看历史对局
  - 支持进入单局详情页查看完整记录
- [ ] 单局详情页支持查看完整对局过程
  - 显示对局基础信息（开始时间、结束时间、玩家、版本号等）
  - 显示行动记录、用时记录、回合结算记录
  - 为后续复盘与争议处理提供依据

### 行动记录系统
- [ ] 建立完整行动流水记录
  - 记录行动发起玩家、所属回合、行动顺序、行动名称、执行时间
  - 记录行动前后关键状态变化，便于审计与复盘
- [ ] 增加行动记录筛选能力
  - 支持按策略筛选
  - 支持按大类 / 小类筛选
  - 支持按行动名称筛选
- [ ] 支持回溯到指定行动
  - 可以定位到某一步具体行动
  - 可以查看该行动前后的状态变化
- [ ] 支持可审计的增量更新
  - 后续补记、修正、系统结算写入时保留变更痕迹
  - 保证记录可追踪、可核对
- [ ] 回合效果结算归入新的 `sys` 标签记录
  - 将系统自动触发的收益、扣减、回合结算与玩家主动行动区分开
  - 提高日志可读性，方便后续分析

### 用时记录
- [ ] 记录每个玩家或每步行动的用时数据
- [ ] 在历史记录详情中展示关键用时信息
- [ ] 为后续统计、复盘与中台分析预留数据基础

### 前端页面与交互
- [ ] 历史记录列表页 UI
  - 支持列表展示、基础筛选与进入详情
- [ ] 历史记录详情页 UI
  - 展示行动时间线、筛选区、详情区
  - 支持跳转到指定行动位置
- [ ] 历史记录与当前对局界面的入口联动
  - 游戏结束后可直接前往历史记录查看
  - 主界面需提供稳定的历史记录入口

### 后端与数据结构
- [ ] 设计对局记录存储结构
  - 明确基础信息、行动日志、用时日志、系统结算日志的数据模型
- [ ] 实现游戏结束后的自动保存流程
  - 在结束游戏逻辑中触发保存
  - 保存完成后返回可用于查询的记录标识
- [ ] 提供历史记录查询接口
  - 支持历史对局列表查询
  - 支持单局详情查询
  - 支持按行动定位或分页读取详细日志

## 玩家面板

- [ ] 玩家面板按行动顺序排序
- [ ] 支持显示已略过玩家状态
  - 明确标识本轮已跳过 / 已结束行动的玩家
  - 方便判断当前轮次推进情况

## 控制中台

- [ ] 支持暂停计时
  - 暂停后可恢复
  - 需要与当前读秒状态联动
- [ ] 支持撤销操作
  - 撤销后需要与行动记录保持一致
  - 需要明确可撤销范围与限制
- [ ] 支持延长读秒
  - 可对当前玩家追加操作时间
  - 需要明确触发入口与显示方式
- [ ] agent 指标构建
  - 为系统侧行为、流程效率与辅助决策预留统计指标

## 游戏区域补充

- [ ] 科学轨道显示
- [ ] 魔力 / 书行动展示与交互
- [ ] 初始板块选择流程与界面
- [ ] 桥和侧楼相关展示与交互

