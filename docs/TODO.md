# 游戏开发待办事项

## 前端界面

### 地图增量渲染
- [ ] 侧楼建筑增量更新按独立渲染单元处理
  - `map_grid` 前端渲染按 3 部分理解：地块、主建筑、是否有侧楼
  - `terrain` 只刷新六边形地块颜色
  - `controller + building_id + is_neutral` 作为主建筑整体重绘
  - `has_annex` 单独控制侧楼叠层的增删，暂缓实现

### 游戏菜单功能
- [x] 右上角选择按钮去掉边框
- [x] 点击按钮使用 Modal 弹窗组件
- [x] 添加"结束游戏"按钮 - 返回主界面
- [x] 添加"重新设置"按钮 - 返回设置页面，保留当前设置
- [ ] **重新开始** - 在当前界面重启游戏（需要后端支持）
  - 需要后端 API 支持重置游戏状态
  - 前端需要重新初始化游戏数据
  - 考虑是否需要确认对话框

### 初始设置页面
- [ ] **电脑策略选择**
  - 简单模式：AI 随机选择行动
  - 普通模式：AI 基于基础策略
  - 困难模式：AI 使用优化算法
  - 需要设计策略选择 UI（下拉框或按钮组）
  - 需要将策略参数发送到后端

- [ ] **自定义游戏模式弹窗**
  - 自定义地图配置（可选地形、大小）
  - 自定义胜利条件
  - 自定义初始资源
  - 需要设计弹窗内的表单布局
  - 需要保存自定义配置到本地存储

## 后端接口

### 游戏控制接口
- [ ] `POST /api/game/restart` - 重新开始游戏
  - 重置游戏状态到初始状态
  - 保留当前玩家设置（玩家数量、派系等）
  - 返回新的游戏会话 ID

### AI 策略接口
- [ ] `POST /api/game/ai-strategy` - 设置 AI 策略
  - 接收策略类型参数（easy/normal/hard）
  - 应用到对应电脑玩家

## 其他待办

- [ ] 添加游戏音效
- [ ] 优化移动端适配
- [ ] 添加游戏存档功能

## 游戏历史记录（回放功能）

> **状态**：规划中，后续版本实现

### 功能需求
- [ ] 记录每回合完整游戏状态
- [ ] 支持任意回合跳转回放
- [ ] 支持快进/快退/暂停控制
- [ ] 显示回合时间轴

### 技术方案
- [ ] 设计历史状态存储结构（内存/数据库）
- [ ] 实现 `GameStateSyncManager._save_to_history()` 方法
- [ ] 实现 `get_state_at_version(version)` 查询接口
- [ ] 前端回放控制UI设计
- [ ] 回放模式与实时模式的状态切换
